# ── Data & preprocessing ──────────────────────────────────────────────────────
pandas>=2.0.0
numpy>=1.24.0
pyyaml>=6.0
nltk>=3.8.0

# ── Classical ML (TF-IDF path) ────────────────────────────────────────────────
scikit-learn>=1.3.0
joblib>=1.3.0
imbalanced-learn>=0.11.0   # SMOTE oversampling

# ── Visualization ─────────────────────────────────────────────────────────────
matplotlib>=3.7.0
seaborn>=0.12.0

# ── Deep learning (DistilBERT path) ───────────────────────────────────────────
# PyTorch must be installed separately to get the right CUDA version for your GPU.
# CPU-only (no GPU):
#   pip install torch
# GPU (CUDA 12.x):
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
# See: https://pytorch.org/get-started/locally/
torch>=2.0.0
transformers>=4.40.0       # DistilBERT, Trainer, EarlyStoppingCallback
accelerate>=0.24.0         # required backend for HuggingFace Trainer
